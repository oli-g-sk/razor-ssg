## How to use

1. Use the `Data` folder to store any structured data or plaintext
2. Deserialize in `Program.cs`
3. If more pages than `index.cshtml` are needed, make sure to **set their Build Action to copy to output folder**